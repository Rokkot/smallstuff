import { async, ComponentFixture, TestBed } from '@angular/core/testing';
/*
import { SmartPopoverComponent } from './smart-popover.component';

describe('SmartPopoverComponent', () => {
  let component: SmartPopoverComponent;
  let fixture: ComponentFixture<SmartPopoverComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SmartPopoverComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SmartPopoverComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/